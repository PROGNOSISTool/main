learner:
    runsPerQuery: 3
    confidence: 85
    maxAttempts: 100
    sutPort: 3333
    sutIP: adapter
    inputAlphabet:
        - INITIAL(?,?)[CRYPTO]
        # - INITIAL(?,?)[ACK,HANDSHAKE_DONE]
        - HANDSHAKE(?,?)[ACK,CRYPTO]
        # - HANDSHAKE(?,?)[ACK,HANDSHAKE_DONE]
        # - SHORT(?,?)[ACK,MAX_DATA,MAX_STREAM_DATA]
        - SHORT(?,?)[ACK,STREAM]
        # - SHORT(?,?)[ACK,HANDSHAKE_DONE]

        # - SYN(?,?,0)
        # - SYN+ACK(?,?,0)
        # - ACK(?,?,0)
        # - ACK+PSH(?,?,1)
        # - FIN+ACK(?,?,0)
        # - RST(?,?,0)
        # - ACK+RST(?,?,0)
adapter:
    http3: 1
    tracing: 0
    waitTime: 400ms
    httpPath: /
synthesizer:
    protocol: QUIC
    dot: quic.dot
    oracle_table: oracleTable.json
